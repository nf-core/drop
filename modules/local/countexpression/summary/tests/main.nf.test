nextflow_process {

    name "Test Process COUNTEXPRESSION_SUMMARY"
    script "../main.nf"
    process "COUNTEXPRESSION_SUMMARY"

    tag "modules"
    tag "modules_local"
    tag "COUNTEXPRESSION_SUMMARY"

    test("drop demo - ods_unfitted.Rds") {

        when {
            process {
                """
                input[0] = Channel.of([
                    [ id: 'test' ],
                    [ file(params.pipelines_testdata_base_path + 'intermediates/filtercounts/ods_unfitted.Rds', checkIfExists: true) ],
                    [ file(params.pipelines_testdata_base_path + 'intermediates/bamcoveragestats/outrider.tsv', checkIfExists: true) ]
                ])
                """
            }
        }
        then {
            assertAll (
                { assert process.success },
                { assert snapshot(
                    process.out.sample_count.collect { meta, r -> [ meta, file(r).name ] },
                    process.out.mapped_vs_counted.collect { meta, r -> [ meta, file(r).name ] },
                    process.out.size_factors.collect { meta, r -> [ meta, file(r).name ] },
                    process.out.reads_statistics.collect { meta, r -> [ meta, file(r).name ] },
                    process.out.meanCounts.collect { meta, r -> [ meta, file(r).name ] },
                    process.out.expressedGenes.collect { meta, r -> [ meta, file(r).name ] },
                    process.out.expressed_genes.collect { meta, r -> [ meta, file(r).name ] },
                    process.out.expression_sex.collect { meta, r -> [ meta, file(r).name ] },
                    process.out.versions
                ).match() }
            )
        }
    }
        test("homo_sapiens - gtf - stub") {

        options "-stub"

        when {
            process {
                """
                input[0] = Channel.of([
                    [ id: 'test' ],
                    [ file(params.pipelines_testdata_base_path + 'intermediates/filtercounts/ods_unfitted.Rds', checkIfExists: true) ],
                    [ file(params.pipelines_testdata_base_path + 'intermediates/bamcoveragestats/outrider.tsv', checkIfExists: true) ]
                ])
                """
            }
        }

        then {
            assertAll (
                { assert process.success },
                { assert snapshot(process.out).match() }
            )
        }
    }
}
