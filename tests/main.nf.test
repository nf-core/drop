nextflow_pipeline {

    name "Test Workflow main.nf"
    script "../main.nf"

    test("Aberrant Expression Workflow") {

        when {
            params {
                outdir = "${outputDir}"
                ae_run = true
                as_run = false
                mae_run = false
            }
        }

        then {
            assertAll(
                { assert workflow.success },
                { assert snapshot(
                    getAllFilesFromDir(params.outdir, relative:true, ignoreFile: 'tests/.nftignore')
                ).match() }
            )
        }

    }

    test("Aberrant Splicing Workflow") {

        when {
            params {
                outdir = "${outputDir}"
                ae_run = false
                as_run = true
                mae_run = false
            }
        }

        then {
            assertAll(
                { assert workflow.success },
                { assert snapshot(
                    getAllFilesFromDir(params.outdir, relative:true, ignoreFile: 'tests/.nftignore')
                ).match() }
            )
        }

    }

    test("Mono Allelic Expression Workflow") {

        when {
            params {
                outdir = "${outputDir}"
                ae_run = false
                as_run = false
                mae_run = true
            }
        }

        then {
            assertAll(
                { assert workflow.success },
                { assert snapshot(
                    getAllFilesFromDir(params.outdir, relative:true, ignoreFile: 'tests/.nftignore')
                ).match() }
            )
        }

    }

}
